# Security

mew is safe-by-default.

Rules:
- never run destructive commands without approval
- never edit outside workspace by default
- never read secrets without explicit permission
- always show diffs before applying patches
